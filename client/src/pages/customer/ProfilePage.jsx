import { useState } from 'react';import { useAuth } from '../../context/AuthContext';import PageHeader from '../../components/PageHeader';import { api } from '../../api/client';
export default function ProfilePage(){const {user}=useAuth();const p=user.customerProfile;const [form,setForm]=useState({currentPassword:'',newPassword:''});const [msg,setMsg]=useState('');const change=async e=>{e.preventDefault();setMsg('');try{await api.post('/auth/change-password',form);setMsg('Password changed successfully.');setForm({currentPassword:'',newPassword:''})}catch(err){setMsg(err.response?.data?.message||'Could not change password.')}};return <><PageHeader title="My Profile" subtitle="Identity and account information linked to your player record."/><div className="grid gap-6 xl:grid-cols-2"><div className="panel p-5"><div className="eyebrow">Player details</div><div className="mt-5 grid gap-4 sm:grid-cols-2">{[['Customer code',p.customerCode],['Display name',p.displayName],['Favourite team',p.favoriteTeam||'—'],['Phone',p.phone||'—'],['Email',p.email||'—'],['Username',user.username]].map(([k,v])=><div key={k} className="rounded-xl bg-white/[.035] p-4"><div className="text-xs uppercase tracking-wider text-white/30">{k}</div><div className="mt-1 font-bold">{v}</div></div>)}</div></div><form onSubmit={change} className="panel p-5"><div className="eyebrow">Security</div><h2 className="mt-1 mb-5 text-xl font-black uppercase">Change password</h2>{msg&&<div className="mb-4 rounded-xl bg-white/[.05] p-3 text-sm">{msg}</div>}<label className="label">Current password</label><input required type="password" className="field mb-4" value={form.currentPassword} onChange={e=>setForm({...form,currentPassword:e.target.value})}/><label className="label">New password</label><input required minLength="10" type="password" className="field" value={form.newPassword} onChange={e=>setForm({...form,newPassword:e.target.value})}/><button className="btn-primary mt-5">Update password</button></form></div></>}
