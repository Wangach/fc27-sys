import { useEffect,useState } from 'react';import { api } from '../../api/client';import PageHeader from '../../components/PageHeader';import StatCard from '../../components/StatCard';import { faGamepad,faTrophy,faArrowTrendUp,faCoins } from '@fortawesome/free-solid-svg-icons';import { kes,dateTime } from '../../utils/format';import StatusPill from '../../components/StatusPill';
export default function CustomerDashboard(){const [data,setData]=useState(null);useEffect(()=>{api.get('/customers/me').then(r=>setData(r.data));},[]);if(!data)return <div className="text-volt">Loading your player profile…</div>;const s=data.stats||{rank:'—',played:0,wins:0,losses:0,draws:0,winRate:0,points:0};return <><PageHeader eyebrow="PLAYER HUB" title={data.profile.displayName} subtitle={`${data.profile.customerCode} • Favourite team: ${data.profile.favoriteTeam||'Not set'}`}/><div className="panel panel-cut relative mb-6 overflow-hidden p-6 md:p-8"><div className="absolute -right-10 -top-16 text-[240px] font-black leading-none text-volt/[.035]">{s.rank||'0'}</div><div className="relative grid gap-6 md:grid-cols-[190px_1fr]"><div className="grid min-h-56 place-items-center rounded-2xl border border-volt/30 bg-gradient-to-br from-volt/20 to-black/20 p-4 text-center shadow-glow"><div><div className="text-6xl font-black text-volt">{s.winRate}</div><div className="text-sm font-black uppercase tracking-[.2em]">Win Rate</div><div className="mt-3 text-xs text-white/40">Rank #{s.rank||'—'} • {s.points} pts</div></div></div><div><div className="eyebrow">Season performance</div><h2 className="mt-1 text-4xl font-black uppercase">Player Card</h2><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><div className="rounded-xl bg-white/[.04] p-4"><div className="text-2xl font-black">{s.played}</div><div className="text-xs text-white/35">Played</div></div><div className="rounded-xl bg-white/[.04] p-4"><div className="text-2xl font-black text-volt">{s.wins}</div><div className="text-xs text-white/35">Wins</div></div><div className="rounded-xl bg-white/[.04] p-4"><div className="text-2xl font-black">{s.draws}</div><div className="text-xs text-white/35">Draws</div></div><div className="rounded-xl bg-white/[.04] p-4"><div className="text-2xl font-black">{s.losses}</div><div className="text-xs text-white/35">Losses</div></div></div></div></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard icon={faTrophy} label="Current rank" value={`#${s.rank||'—'}`}/><StatCard icon={faGamepad} label="Games played" value={s.played}/><StatCard icon={faArrowTrendUp} label="Win rate" value={`${s.winRate}%`}/><StatCard icon={faCoins} label="Total debt" value={kes(data.account.totalDebt)} hint={`Game ${kes(data.account.gameDebt)} • Purchases ${kes(data.account.purchaseDebt)}`}/></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="panel p-5"><h2 className="mb-4 font-black uppercase">Recent games</h2><div className="space-y-3">{data.recentMatches.slice(0,5).map(row=>{const m=row.match;const [a,b]=m.participants;return <div className="rounded-xl bg-white/[.035] p-3" key={row.id}><div className="text-xs text-white/30">{dateTime(m.playedAt)} • {m.matchType.name}</div><div className="mt-1 flex items-center justify-between"><div className="font-bold">{a.customer.displayName} <span className="text-volt">{a.score} : {b.score}</span> {b.customer.displayName}</div><StatusPill value={row.result}/></div></div>})}</div></section><section className="panel p-5"><h2 className="mb-4 font-black uppercase">Recent transactions</h2><div className="space-y-3">{data.recentPayments.slice(0,5).map(p=><div className="flex items-center justify-between rounded-xl bg-white/[.035] p-3" key={p.id}><div><div className="font-bold">{p.purpose.replaceAll('_',' ')}</div><div className="text-xs text-white/30">{dateTime(p.createdAt)} • {p.method}</div></div><div className="font-black text-volt">{kes(p.amount)}</div></div>)}</div></section></div></>}
